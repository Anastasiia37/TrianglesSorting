This application performs the output of triangles in descending order of their area. After adding each new triangle, the program asks if the user wants to add one more. If the user answers “y” or “yes” (case-insensitive), the program will ask you to enter data for another triangle, otherwise it will output the result to the console.

Input parameters: name of triangle,length,length,length